apiVersion: apps/v1
kind: Deployment
metadata:
  name: fm-compta-consulting-backend
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fm-compta-consulting-backend
  template:
    metadata:
      labels:
        app: fm-compta-consulting-backend
    spec:
      containers:
        - name: backend
          image: ghcr.io/rolandsebastien/fm-compta-consulting-backend:bdf81db7c17d15b076b3b73fff720603fd7a8a33
          ports:
            - containerPort: 3001
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "3001"
            - name: MONGODB_URI
              value: "mongodb://admin:password@database:27017/compta_db?authSource=admin"
            - name: DATABASE_URL
              value: "mongodb://admin:password@database:27017/compta_db?authSource=admin"
            - name: JWT_SECRET
              value: "votre_secret_jwt_securise_123456789"
